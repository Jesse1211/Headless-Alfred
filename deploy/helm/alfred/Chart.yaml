apiVersion: v2
name: alfred
description: |
  Headless Alfred — single-pod web shell + Claude UI.
  One environment for now (oracle k3s, private only, no TLS), per
  values-oracle.yaml. See deploy/README.md.
type: application
# Chart shape version — bump on template/values shape changes.
version: 0.1.0
# App version — the Alfred release running in the pod. Bumped at
# every release per RELEASE.md. Should match the matching git tag.
appVersion: "v0.1"
home: https://github.com/Jesse1211/Headless-Alfred
sources:
  - https://github.com/Jesse1211/Headless-Alfred
maintainers:
  - name: Jesse Liu
    email: zl942@cornell.edu
