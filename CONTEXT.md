# Headless Alfred — Context

This document is for someone (human or LLM) about to work on this codebase. It captures the **why** and the **non-obvious**, not the **what** — the code already tells you the latter.

If you have less than 5 minutes, read just **Mental model** and **Three invariants that don't bend**.

---

## Mental model

One Go process per Pod. That process owns:

1. **One bash subprocess** spawned at startup with `--noprofile --norc` and empty `PS1`. It lives until the Go process dies.
2. **One PTY** that bash's stdin/stdout flow through. Output is parsed via sentinel markers (`\x1eALFRED_START_<nonce> <cmdID> <cwd>\x1e`) wrapped around each user command. **PTY echo is disabled at startup** (`stty -echo`) so the wrapper script we feed bash isn't echoed back into the parser; see the traps table below.
3. **Two broadcasters** (raw bytes + typed events). Subscribers come and go (WebSocket connections, the disk-writer). The bash process never blocks on a slow subscriber.
4. **A JSON file store** at `/data` — one file per command for metadata, one for output. Atomic writes via tmp+rename.
5. **An HTTP+WS surface** for the React client.

The React app is just a viewer. **All state of consequence lives in the Go process and on the PVC.** Close the browser → bash keeps running → reconnect → reattach event delivers the in-memory buffer so far.

---

## Four invariants that don't bend

If you find yourself violating any of these, stop and think.

### 1. Bash lifecycle ≠ WebSocket lifecycle.

Bash is born when Go starts and dies when Go stops. The set of WebSocket connections is orthogonal. If you add code that ties a bash command to a specific connection, you have introduced a regression that breaks the entire premise of the project (long-running jobs surviving a closed laptop).

**Strengthening (multi-session):** Bash lifecycle ≠ Go-process lifecycle either. The tmux server outlives alfred-server. `kubectl rollout` does NOT terminate in-flight commands; the in-flight command keeps streaming into `pty.stream`, and the new alfred-server process resumes parsing it from `pty.offset` and re-emits any pending `Started`/`Ended` events to reconnecting clients. Pod restart DOES terminate them; this is an accepted trade-off documented in spec §1 non-goals.

### 2. Output buffer lives where commands live: in `shell.Shell`.

The `EndedEvent.Output` field carries the buffer at the moment of completion. **Do not** try to read the buffer via `Shell.CurrentCommand()` after a command ends — `currentCmd` is already nil by then. This bit us during Plan 2; the fix was to attach the buffer to the event itself.

### 3. `waitLoop` must atomically mark the shell unavailable.

When bash dies, `waitLoop` must clear `currentCmd`, `available`, `cmd`, `pty` **under the same lock** before releasing it to publish the Ended event. Otherwise a concurrent `Write()` between unlock and the restart's re-lock can sneak in, write to a closed PTY, and brick the shell with an orphan command that no event will ever clear. There's a comment in `internal/shell/shell.go` `waitLoop` explaining this; respect it.

### 4. Every Claude-UI turn-terminator must route through `claudestate.Apply`. Emitting a WS frame is not enough.

The frontend's spinner / composer-locked state derives from `ClaudeState.InFlight` + `turns[-1].Done`, hydrated from the **server's** in-memory state on every reconnect. If a terminator (a `result` event, a `claude_run_ended` from the reaper, a synthesized `claude_error` on spawn failure, the disconnect-cleanup path SIGINT'ing leaked runners, the per-session deletion path, the graceful-shutdown finalize) writes only a WS frame and skips `Apply`, the live client may see the frame fly by — but **the next reconnect's `/claude-state` hydrate still shows `InFlight=true` and the trailing turn `Done=false`**. The composer locks forever, the spinner spins forever, and the only fix is a server restart's `finalizeStaleTrailingTurn` fallback.

This was the original "Claude stopped but server still running" bug class. Every audit that finds a turn-end path which doesn't go through `SessionState.Apply` is finding a regression. The pattern is: `applyClaudeRunEnded(sid, reason, mgr)` updates state silently (for paths with no live client); `dispatchClaudeRunEnded` / `dispatchClaudeError` do both Apply AND emit a frame (for paths where a live client is reading). Pick the right one — never emit-only.

---

## Why these choices

| Choice | Why |
|---|---|
| Single user, hardcoded credentials | Personal tool. Per spec §8: K8s Secret env vars, static long-random token, no JWT/session. Acceptable trade-off given Secret rotation = pod restart. |
| Up to 8 bash sessions, one command at a time per session | Each session is an isolated tmux session (own cwd / env / aliases) but shares the container FS. Concurrency *within* a session is what `&` is for; concurrency *across* sessions is what the sidebar is for. Was originally single-bash (spec §3, since superseded by `docs/superpowers/specs/2026-06-11-multi-session-tmux-design.md`). |
| Output **first** 10 MB kept, not last | When a `npm run train` blows up, the head of output usually contains the actual failure. Simpler than rolling truncation. |
| `OutputPath` removed from `Record` | Was duplicated state. Path is deterministic — `store.outputPath(id)`. Storing it caused a lost-update race between `Save(rec)` and `WriteOutput(id)`. |
| Stop = SIGKILL bash + restart | We tried SIGINT through the PTY — terminal line discipline on macOS dropped queued sentinel printfs and we never got a clean END event. Killing bash is simple, reliable, and produces a non-zero exit code. Trade-off: cwd/env/aliases reset on Stop. |
| Trust `X-Forwarded-For` blindly | Traefik terminates TLS in front; the Service is only reachable through it; XFF is set by Traefik. If you ever expose the Service directly (don't), revisit this. |
| Skip Ingress in kind E2E | Let's Encrypt cannot issue against `localhost`. We port-forward the Service to `127.0.0.1:18080` and test the inner stack. Ingress correctness is verified manually in production. |
| `USER 1000` (numeric) in Dockerfile | `runAsNonRoot: true` cannot verify a user named `alfred` without introspecting `/etc/passwd` inside the image. Use a numeric UID and the orchestrator agrees with the image. |
| Bg-task lifecycle is CLI-owned; alfred is observer-only | Manual spike with `claude -p` 2.1.169 on macOS confirmed CLI parents bg bashes as children and SIGKILLs them at `claude -p` exit. A detached-bash + alfred-fosters model would require either tmux containerization (CLI still kills) or `updatedInput` command rewriting (relies on an unverified CLI feature) — both fragile. Observer-only model is honest and works against the CLI as shipped. See `DESIGN.md` (UI Background Tasks Panel) ADR-001. |

---

## Non-obvious traps we already fell into

If you change any of these, you will probably hit the same bug we already fixed. Each has a regression test guarding it.

| Trap | What happens if you forget | Test |
|---|---|---|
| `http.FileServer` 301-redirects any path ending in `/index.html` to the parent | SPA routes like `/login` bounce forever | `TestHandler_SPAFallback_DoesNotRedirect` |
| `statusRecorder` (RequestLogger's wrapper) hides the underlying `http.Hijacker` | Every WebSocket upgrade returns 500 with "response does not implement http.Hijacker" | `TestRequestLogger_PreservesHijacker` |
| `go:embed dist` skips dotfiles by default | An empty `dist/` (only `.gitkeep`) reports "no embeddable files" at compile time | The Dockerfile populates `dist/` with the real build; locally use `make embed-web` |
| `printf` with a `\x1e` followed by raw text confuses the parser if the next byte isn't a known terminator | We append a literal `X` after the closing RS to give the parser a definitive end-of-sentinel marker | `TestSentinelParser_HandlesSentinelSplitAcrossFeeds` |
| Login rate limiter is per-IP (`X-Forwarded-For` from Traefik) | Tests that all log in from `127.0.0.1` burn the same bucket | E2E tests set `X-Forwarded-For` per `t.Name()`; production deals with this naturally |
| Bash PTY echo is on by default — every byte written into the master is echoed back through the slave | The echoed wrapper (`printf 'START…'`, the user command, `__alfred_ec=$?`, `printf 'END…'`) arrives **after** the START sentinel has flipped the parser to `stateInside`, so it leaks into the visible output as garbage like `__alfred_ec=$?` and `printf '\\x1eALFRED_END_…'` | `startLocked` writes `stty -echo\n` to the PTY immediately after `pty.Start`; that command's own echo lands in `stateOutside` and is discarded |
| The WS layer can deliver the SAME `(started, chunk, done)` triple twice for one cmdId — React StrictMode double-fires effects in dev, multi-tab subscriptions multiply broadcasts, and any future reconnect-replay path would too. The naive reducer would (1) double-render the user's command, (2) leave a phantom "● live" turn when a later `done` sees `messages.has(cmdId)` and early-returns without clearing the resurrected `running`, and (3) make Stop look dead because it points at a cmdId the backend already finished (409 not_running, swallowed by useSessions.stop's try/catch) | User reports: one command renders two turns, instant-return commands stay pinned on live, Stop button does nothing. All three symptoms have the same root cause | `sessionsReducer.ts` does defensive dedup in three spots: `started` no-ops if cmdId is already in `messages` or already the live `running`; `chunk` skips a chunk whose bytes are already the tail of the running buffer; `done` clears any stale phantom `running` with the matching id even when it doesn't append a new message. Asserted by `sessionsReducer.test.ts` (~6 cases) and by `web/e2e/regression.spec.ts` (Playwright — three browser-driven regression tests with screenshots, run via `npm run test:e2e`) |
| `tmux send-keys -l` sends `\n` as a literal character, not Enter | bash never executes the wrapper-script line; sentinel never fires; UI hangs forever waiting for `Started` | `TmuxRunner` splits into `SendText` + `SendEnter`; covered by `TestExecRunner_SendTextThenEnter_ExecutesCommand` |
| `tmux pipe-pane` writer holds `pty.stream` open across a naive rename, so output ends up in the unlinked inode | Subsequent commands' bytes silently lost; persisted output appears truncated | `StreamReader.TruncateConsumed` does stop-pipe → truncate → restart-pipe; covered by `TestStreamReader_TruncateAtIdleBoundary` |
| A FIFO consumer disappearing during Go restart SIGPIPEs bash → tmux session dies → violates invariant #1 | Lost session + lost in-flight command on every Go restart | spec §3 chose regular-file + byte-offset over FIFO; covered by `TestStreamReader_ResumesFromPersistedOffset` and the E2E `TestE2E_GoRestart_DuringStreamingChunks` |
| bash on a PTY runs interactive: it emits readline bracketed-paste sequences (`\x1b[?2004h`/`l`) and a visible `bash-X.Y$ ` prompt *before* the wrapper's START sentinel, so those bytes land at the tail of the previous command's captured output | Stored output looks like real shell stdout *plus* mystery escape codes and partial prompts; users complain the UI doesn't match a terminal | After `stty -echo`, also send `bind 'set enable-bracketed-paste off'; PS1=''; PS2=''` — empty prompts have nothing to bleed. Asserted by `TestTmuxShell_Start_CreatesSessionAndConfigures`; verifiable end-to-end with `scripts/multi-session-smoke` |
| WSHandler snapshots `m.List()` at connect time and only subscribes to those sessions — new sessions created later via REST get no event path back to the open WS | Browser submits a `run` for the just-created session, server processes it, but `started`/`chunk`/`done` never reach the client. User sees a blank chat until they reload | `Manager.AddCreateListener` + a per-WS forwarder goroutine pushes the new session's events onto the existing FanIn output channel and writes an `idle` frame. Asserted by `scripts/new-session-ws` |
| `ansi-to-html` defaults `escapeXML: false` — it leaves angle brackets alone and only wraps SGR codes in `<span>` | A command output containing literal `<script>...</script>` injects DOM via `dangerouslySetInnerHTML` in `ChatStream` | `new AnsiToHtml({ escapeXML: true, ... })` in `ChatStream.tsx`; XSS guard asserted by `ChatStream.test.tsx` "HTML-escapes literal angle brackets" |
| `git commit` without `-m` launches `$EDITOR` (vi); we don't ship vi. bash hangs forever waiting for an editor that never appears, leaving `.git/COMMIT_EDITMSG` behind | UI shows a "running" command that will never end; Stop button is the only way out, and it SIGKILLs bash (resets cwd/env/aliases) | `validateGitCommit` in `internal/api/git_commit_guard.go` short-circuits at WS input validation; catches `-m foo`, `-m"foo"`, `-mfoo`, `--message foo`, `--message=foo`. Asserted by `git_commit_guard_test.go` and `TestE2E_GitCommit_RequiresDashM` |
| In claude mode, sentinel-parser frames (`started`/`chunk`/`done`) must NOT ship to the client, otherwise the chat-stream view re-mounts with garbage Claude TUI bytes | Browser sees a phantom "command" with claude's box-drawing bytes leaking into ChatStream | `ws.go` checks `m.GetMode(ev.SessionID) == store.ModeClaude` and `continue`s before calling `writeEventToClient`; the parser keeps running so nothing else breaks |
| `Session.Mode` is persisted in `sessions.json` but MUST be reset to `shell` in `Manager.Reconcile`'s "stored \\ live" branch | After a Pod restart, the Go process sees `mode:claude` in sessions.json but the actual Claude process is dead — WS handler dispatches stdin/pty_data frames pointing at nothing | `Manager.Reconcile` calls `SetMode(id, ModeShell)` for every stored-but-not-live session before persisting. Asserted by `TestReconcile_ResetsClaudeToShell` |
| Bare `claude` typed in the chat input runs as a normal bash command; its TUI bytes (cursor-positioning `\x1b[<col>G`, alt-screen `\x1b[?2004h`, etc.) leak into the ChatStream view, which only renders SGR colors. Result: a screen full of stray `G` characters, "live" indicator stuck on, and a session you can't easily recover from | User sees junk like `G❯G2.GDarkGmodeG✔` instead of Claude's UI; cwd/env preserved but visually broken | `WorkspacePage`'s `onSubmit` intercepts ANY command that starts with `claude` / `/claude` (with or without args) and routes to `enterClaude()` instead of `submit()`. Otherwise we'd have to render a full VT100 emulator inside ChatStream, which xterm.js already does in the dedicated `ClaudeTerminal` |
| `tmux #{pane_pid}` returns the pane's ORIGINAL process (the bash that tmux started), NOT the current foreground process | SIGKILLing pane_pid kills bash → fires `PaneDead` → `OnUserExit` → Manager closes the whole session. Anyone trying to "kill just claude" by inspecting pane_pid and sending SIGKILL will accidentally destroy the session | `TmuxShell.ExitClaude` reuses the same kill+respawn-pane flow as `Stop()` (sets `stoppingForRespawn`, kills bash, respawns a fresh one, reconfigures). The trade-off: cwd/env are LOST on Exit Claude — same as Stop. Documented in README |
| Claude UI mode uses `claude -p --output-format stream-json --include-partial-messages --verbose --dangerously-skip-permissions [--session-id\|--resume] <uuid>`. The CLI is asymmetric: `--session-id` only works the FIRST invocation (creates the transcript; second call with same UUID errors "Session ID X is already in use" → exit 1); `--resume` only works AFTER a transcript exists ("No conversation found" otherwise). `--dangerously-skip-permissions` is NOT "skip our hook" — it only skips the CLI's built-in prompt; PreToolUse hook still fires (verified empirically), so Alfred remains the single source of permission truth | Passing the wrong flag silently shows up as "exit status 1" in the chat for every prompt past the first. Confusing because it's not a CLI crash, it's a transcript-state mismatch | `internal/claude/runner.go::transcriptExists(cwd, uuid)` probes `$HOME/.claude/projects/<cwd-with-/-replaced-by-->/<uuid>.jsonl` and picks `--resume` if present, `--session-id` otherwise. The UUID lives in `SessionMeta.ClaudeSessionID` (persisted) and is allocated on the first UI-mode prompt by `Manager.EnsureClaudeConvoID`. `Reconcile` does NOT clear it — the transcript survives Pod restarts, so the conversation continues from where it left off |
| In Claude UI mode, tool approvals route through a Bridge HTTP server (127.0.0.1:8090) that the PreToolUse hook curl-POSTs to. The hook blocks synchronously on stdout until Alfred resolves; routing chain is `tool_use_id → bridge.PendingRequest → dispatcher → Alfred sessionID → WS subscriber`. The hook response MUST be the nested `{"hookSpecificOutput":{"hookEventName":"PreToolUse","permissionDecision":"allow"\|"deny","permissionDecisionReason":"..."}}` shape — the old flat `{"permissionDecision":"..."}` is silently ignored by claude 2.1.142 (treated as no decision → bypass falls through to default-allow) | Returning the flat shape makes every Allow/Deny click silently no-op; AskUserQuestion answers never reach Claude | `internal/claude/bridge.go::writeHookResponse` always nests under `hookSpecificOutput`. Asserted by `bridge_test.go`'s `parseHookResponse` helper |
| `~/.claude/settings.json` is **user-global** — hook installs once and every `claude` process on the machine reads it, including the developer's own shell-CLI claude. If the dispatcher denied unknown sessions, Alfred would lock the developer (or any concurrent `claude`) out of every tool call | Booting `alfred-server` on dev machine bricks every other claude instance until the developer manually wipes `hooks.PreToolUse` from settings.json. The author has personally been bitten by this | `internal/claude/dispatcher.go::OnAsk` is fail-open: unknown Claude convo (lookup returns "") → `autoAllow`, not `autoDeny`. Only known-but-no-subscriber and queue-full deny. Regression-guarded by `TestDispatcher_UnknownClaudeConvo_AutoAllow` |
| The hook command path differs between pod and dev: pod has `/usr/local/bin/alfred-claude-bridge` baked in by Dockerfile, dev machines don't (would need sudo). Without a fallback, dev users can't run UI mode at all | First UI prompt errors with "fork/exec /Users/.../alfred-claude-bridge: no such file" — and no install instructions in any README | `internal/claude/settings.go::resolveBridgePath(home)` prefers the prod path when it exists, otherwise writes a copy of `bridgeScriptBody` to `$HOME/.local/share/alfred/alfred-claude-bridge` (chmod 0755, idempotent on every UI-mode entry) |
| Claude UI mode's renderer (`tui` vs `ui`) is **locked at `enter_claude` entry**, never switched in place. Sidebar nav between sessions is free; switching renderer requires Exit Claude + re-enter | If you try to flip `ps.renderer` mid-conversation, the `claude` process state (UI: forked per-prompt runners; TUI: tmux pane) won't match the new view and one of them goes orphaned | `StartClaudeDialog` is the only entry point that sets renderer; `handleEnterClaude` dispatches by it. `Manager.Reconcile` clears `Renderer` on Pod restart (process is dead) but preserves `ClaudeSessionID` (transcript survives) — so the next `Start Claude` shows the dialog again but the UUID gets reused and the conversation continues from disk |
| Claude UI mode's `turns` array is **frontend-only state**. The backend never persists them — `claude_event` frames stream live, the reducer accumulates them into `perSession[sid].claude.turns`, and a page reload empties the Map. Source-of-truth recovery comes from Claude CLI's own jsonl at `~/.claude/projects/<encoded-cwd>/<uuid>.jsonl` — parsed by `internal/claudehistory` and surfaced via `GET /api/sessions/{sid}/claude-history`. The hook `useClaudeHistoryLoader` fetches once per session per page lifecycle, gated by `claude.turnsLoaded`, and **preserves local turns** when the fetch returns empty (race: the user may have already typed before the fetch settles; backend jsonl flushes lag local state) | Without the local-preserve guard: user enters Claude, types a prompt immediately, the in-flight loader call returns `[]` and overwrites the just-added turn — chat goes blank mid-conversation | `internal/claudehistory/parse.go` defensively skips unknown line types so CLI version bumps don't break parsing; rebuilt tool calls always show `decision: 'allow'` (denied tools surface via the matching `tool_result` with `is_error: true`, lossy but acceptable). Cross-environment (pod ↔ local) is not supported — different `claudeInvocationCWD()` resolves to different jsonl paths |
| Composer Enter semantics are guarded against IME composition. Naive `if (e.key === 'Enter' && !e.shiftKey)` fires once for the keypress that commits the IME selection (Chinese/Japanese/Korean), shipping a half-finished message | User in Chinese IME types pinyin → presses Enter to commit a candidate → message sends with the un-committed pinyin instead of the chosen 汉字 | All three composers (`CommandInput`, `ClaudeChatView` textarea, `SessionsSidebar` rename input) route through `isSubmitKey(e)` in `web/src/lib/keyboard.ts`, which adds `!e.nativeEvent.isComposing`. Asserted by `keyboard.test.ts` — jsdom doesn't honor the `isComposing` constructor option, so the test uses `Object.defineProperty` on a real DOM `KeyboardEvent` |
| Recap sessions are **a different SessionKind** (`store.KindRecap`), accessed only via the `+ 复盘` button in the SessionsSidebar. They are NOT listed by `Manager.List(store.KindChat)` (the sidebar fetch) — the frontend has to inject the recap session meta locally via `setSessionMeta` after `POST /api/recap-sessions`, AND `WorkspacePage` must filter `sessions.filter(s => s.kind !== 'recap')` before passing to `SessionsSidebar` so it doesn't leak into the chat list. On switch-away, a `useEffect` keyed on `prevRecapIdRef` fires `DELETE /api/recap-sessions/current` (idempotent; backend self-heals via `CreateOrGetRecapSession`'s orphan-kill path) | Without the sidebar filter, "Recap" shows as a chat session in the left panel; without the auto-delete, recap tmux panes accumulate forever as users switch away. Without orphan-kill, two simultaneous `+ 复盘` clicks (multi-tab) leak two recap sessions | `internal/session/manager.go::CreateOrGetRecapSession` scans for existing `KindRecap` sessions and kills all but the most-recently-created. Auto-allow path for the recap file (`<dataDir>/recaps/<YYYY-MM-DD>.md`) lives in `internal/claude/dispatcher_recap.go::isRecapIO` (Read of any date allowed; Write capped at 16KB) |
| The right rail is now a `RightRail` accordion (Summary + Notes), not a `SummarySidebar`. The header `Summary` text button became a `workspace__sidebar-icon-btn` SVG icon that toggles the whole rail. If you re-introduce a "summary-only" predicate (e.g. ` && ps.templateId === 'summary-todo'`) on the WHOLE rail you'll break Notes for sessions without the summary template | Notes panel vanishes for shell sessions or claude sessions without summary opt-in, breaking the feature | `showRightRail = mode is set && !isRecap` is the WHOLE-rail predicate; `showSummarySection` (passed to RightRail as a prop) is the inner-section gate. Toggling the icon only flips `sidebarHidden`; accordion sections inside have their own localStorage keys (`alfred_right_rail_{summary,notes}_collapsed`) |
| Per-session state on the frontend (`perSession[sid]`) is **WS-frame-derived**, not REST-derived. If you add a new `SessionMeta` field that needs to survive a page reload (the user navigates away or hits ⌘R, the WS reconnects fresh, the React Map starts empty), you MUST also echo the field on the `idle`/`reattach` frames and rehydrate it in the matching reducer cases. The list endpoint (`GET /api/sessions`) is read once at boot for the sidebar list, but its results are NOT the source for `perSession` — that's `useSessions.onMessage`'s reducer path. We learned this when `templateId` was only sent on the inbound `enter_claude` frame: after a reload, `ps.templateId` was always undefined, so `showSummarySidebar` was false and the right rail vanished | Reload kills any per-session state that doesn't ride on a re-sent WS frame. Symptom: right-rail sidebar, claude state init, or any conditional UI gated by `ps.<field>` silently drops to its empty-state branch after every refresh | `internal/api/ws.go`'s reattach/idle emitter populates `Mode`, `Renderer`, `TemplateID` from `Manager.GetMode/GetRenderer/GetTemplateID`. Frontend `sessionsReducer.ts::idle`/`reattach` reads them onto `perSession`. Asserted by the manual repro `web/.screenshots/summary-reload` |
| `recap_updated` WS frames are **broadcast to all clients**, not per-session, because recap files are global (one per local date, not bound to a sessionID). The fan-out lives in `internal/api/recap_broadcast.go::recapBroadcaster` — a single fsnotify watcher started in `main.go` feeds many per-WS subscribers. On the frontend, `recapFetchCounter` is **top-level state on `useSessions`**, NOT inside `perSession[sid]`, so any frame bumps the same counter and the `RecapSidebar` effect re-fetches the list + content | Putting the counter inside `perSession` would mean the bump for date X only triggers a refetch on the session whose perSession entry happens to receive it — but recap events have no sessionID at all, so there's nothing to route them to | Reducer treats `recap_updated` as a no-op for `perSession`; the actual counter increment lives in `useSessions.onMessage`'s `if (m.type === 'recap_updated')` branch |
| **Backend serves embedded `internal/static/dist/`, NOT `web/dist/`.** `npm run build` only writes the latter; without copying to `internal/static/dist/` AND rebuilding the Go binary, **the production server keeps serving stale CSS/JS forever**. Vite dev server (5173) is fine — it serves source directly. But hitting 8080 means hitting the embed.FS frozen at the binary's build moment | You change CSS, hard-reload 8080, nothing happens. You blame caches, the bundler, the browser. Real cause: the binary still contains the dist from hours ago. The Dockerfile populates it correctly in prod; locally you have to do it by hand or via `make embed-web` | None — this is a deployment workflow trap, not a code bug. Use 5173 for iterative frontend work; only test 8080 after `npm run build && cp -R web/dist/. internal/static/dist/ && go build -o /tmp/alfred-server ./cmd/alfred-server && restart` |
| **Grid `1fr` is shorthand for `minmax(auto, 1fr)`**, and `auto` means "at least `min-content`". If the row's content has unbreakable intrinsic min-height (long `<pre>` code blocks, fixed-width images, etc.), `1fr` resolves to `max(remaining-space, min-content)` — i.e., it WILL grow past the viewport. `min-height: 0` on the **flex** child has no effect on grid track sizing — that's a different algorithm | Long Claude chats made `.workspace__main` taller than viewport; composer pushed off-screen; user saw "page can scroll down into a huge empty band". 5 fix attempts (`min-height: 0` on every ancestor) all failed because they targeted the wrong sizing algorithm | `.claude-chat` uses `grid-template-rows: minmax(0, 1fr) auto` — the explicit `minmax(0, ...)` is the **only** way to tell grid the row may shrink below its content. Documented in the rule's comment block. Also: `.workspace` itself pins `grid-template-rows: 100vh` + `overflow: hidden` as a viewport-bound safety net |
| **Classic `position: absolute + 1px + clip` visually-hidden recipe needs a `position: relative` ancestor**, otherwise it positions against `<html>`. If the element is rendered inside a scroll container at normal-flow y=2500px, `position: absolute` with no top/left falls back to that y — but **relative to the viewport-root**, not the scroll container. Multiple labels at y=1500, 2000, 2700 inflate `<html>.scrollHeight` to 2700+ even though `body` is still 900. Page silently becomes scrollable; the chat appears "pushed up" with a giant empty band below | `.claude-turn__label` (the screen-reader-only "You" / "Claude" labels) used this recipe — turned out to be the actual root cause of the chat-pushed-up bug, NOT any of the grid-sizing issues I chased first | Use `clip-path: inset(50%)` instead (no positioning at all). Same a11y semantics; zero layout impact. The `.claude-turn__label` rule documents the prior bug in a comment so nobody re-introduces it |
| **Flex with two `margin-left: auto` declarations doesn't produce "left / center / right"** — flexbox splits the remaining space equally between the two autos, so both elements land in the middle. Wanted a header layout `left-cluster · center-button · right-toggle`; ended up with `left-cluster · center-button · center-toggle` (toggle never made it to the far right) | Header had brand+dots on the left, but the sidebar-toggle that "should be on the right" sat awkwardly next to the Claude button in the middle | Use grid `grid-template-columns: 1fr auto 1fr` for header-style 3-region layouts. Each region is geometrically pinned. See `.workspace__header` + `workspace__header-left/center/right` slot divs |
| **`crypto.randomUUID()` is gated on Secure Context** (HTTPS or literal `http://localhost`). `http://alfred.local:8888/` (the ssh-tunnel deploy mode) is NOT a secure context — `alfred.local` is just a `/etc/hosts` alias for `127.0.0.1`, and only the EXACT string `localhost` qualifies. The same restriction applies to `navigator.clipboard.writeText`, Service Workers, Notifications, getUserMedia, etc. | First Claude prompt crashed with `TypeError: crypto.randomUUID is not a function` in `beginClaudeTurn`. Symptoms appear only when accessed via the ssh-tunnel URL; localhost dev (`http://localhost:5173/`) works fine, hiding the problem | `web/src/lib/randomId.ts` exports `randomId()`: prefers `crypto.randomUUID` when available, otherwise falls back to `Date.now() + Math.random()`. Used as a React key within a single tab session, so cryptographic strength is not required. **Future:** any new Web-Crypto / Clipboard / SW API needs a non-secure-context fallback or feature detection, or the production deploy URL breaks for users |
| **Vite proxy + ws upgrade + custom hostname target = the upgrade response never makes it back to the browser.** The `target` of `proxy['/ws']` is the URL of the upstream WebSocket. If you write `target: 'ws://alfred.local:8888'` (hostname that resolves to 127.0.0.1 via /etc/hosts), the http-proxy-middleware *connects* fine over HTTP, but during the WS upgrade dance Node's DNS / dual-stack path injects a delay / second resolution that makes the 101 response stall or get RST somewhere between Traefik and the proxy. Result: ws.readyState stays at CONNECTING forever, no console error, no backend log entry past "ws status: 200" (Traefik did upgrade — the byte never made it back). The HTTP `/api/...` path works because there's no upgrade dance to be desynced | After 30+ minutes chasing CheckOrigin and header rewrites, the fix turned out to be writing `target: 'ws://127.0.0.1:8888'` (literal IPv4) and keeping the Host/Origin rewrite separate via the `configure` callback. Lesson: when DNS is involved in a Node-proxy + WS upgrade chain, prefer the IP literal | `web/vite.config.ts` derives `targetHost = '127.0.0.1'` whenever the backend hostname isn't literally `localhost`; Host + Origin headers are then forced to the real hostname (`alfred.local`, no port) via a `proxyReqWs` event handler in `configure` — the top-level `headers:` shorthand was also broken for WS upgrades in our http-proxy-middleware version |
| **`gorilla/websocket` CheckOrigin enforces `Origin == "scheme://" + r.Host`** by default-ish (our `internal/api/ws.go` implementation does, and that's typical). The Host header is what Traefik strips the port from before forwarding, so the rule effectively becomes "Origin must be `http://alfred.local` (no port)". Vite's `changeOrigin: true` rewrites Host but doesn't touch Origin, so the browser's `Origin: http://localhost:5173` slips through and gets 403'd at the upgrade | "request origin not allowed by Upgrader.CheckOrigin" 403, but only when ws-proxying — not when curling the backend directly with matching headers | Vite proxy config explicitly sets `Origin: http://<backend-hostname>` (port-free) via `proxyReqWs`. Backend code is correct as-is; the trap is on the proxy side. If we ever loosen `CheckOrigin` to allow more origins, do it via an allowlist, not a blanket `return true` |
| **Layered Helm: chart Namespace + `--create-namespace` collide.** Putting a `namespace.yaml` template in the chart AND passing `--create-namespace` makes the second one error with `namespaces "<x>" already exists` on first install. Worse: that error happens AFTER helm has already written rev 1 metadata, so subsequent upgrades see a "stuck" failed release and need a manual `helm uninstall` to recover. NYSM's chart deliberately omits the namespace template for the same reason | First deploy: `Error: 1 error occurred: * namespaces "alfred" already exists` → release rev 1 is created as `failed` → next deploy errors with `services "alfred" not found` because it diffs against a partial rev 1 manifest | Drop the `namespace.yaml` template from the chart; rely entirely on `--create-namespace`. If you need labels on the namespace, write a separate `kubectl label namespace` step in the deploy script — don't put the Namespace resource itself in the chart |
| **Dockerfile pinning `GOARCH=amd64` defeats native ARM builds** on an arm64 server. Symptoms are deceptive: build succeeds, image looks fine, pod scheduling succeeds, then `exec format error` at runtime → CrashLoopBackOff. The build host (oracle box, ARM) has `nerdctl build` natively producing arm64 by default — but TARGETOS/TARGETARCH baked into the Dockerfile ARG with `=amd64` default overrides that | Pod stuck on `cannot execute binary file: Exec format error` in a respawn loop; nothing in build logs explains it | Don't default `TARGETARCH=amd64` (or any arch) in the Dockerfile. Let buildkit's `--platform` negotiation pass the host arch through, with `GOARCH=${TARGETARCH:-$(go env GOARCH)}` as a graceful fallback for plain `go build` |
| **`fsGroup` does NOT propagate to PVC `subPath` mounts.** kubelet only recursively chowns the volume root, not the subdirectory it chrooted into for subPath. With `runAsUser: 1000 + fsGroup: 1000` and a `subPath: home` mount onto `/home/alfred`, the first pod on a fresh PVC sees `/home/alfred` owned by `root:root` mode 0755 — the alfred user can't write. Every $HOME-touching feature (git creds save, Claude creds save, jsonl init) silently fails | First deploy with the subPath mount: anthropic credentials dialog "saves" successfully but `~/.claude/.credentials.json` was actually a permission-denied silent no-op. Looks like a frontend bug; really an ownership bug. Trap because **only happens on FIRST pod after fresh PVC** — once a user with root somehow chowns it, all later pods are fine | One-shot init container `chown-volumes` running as root with `capabilities: [CHOWN, FOWNER, DAC_OVERRIDE]`, mounting the same subPaths the main container will, executes `chown -R 1000:1000 /data /home/alfred && chmod 0700 /home/alfred`, then exits. Main container takes over with the corrected perms. See `deploy/helm/alfred/templates/deployment.yaml` initContainers block |
| **`turn.text` + `turn.tools[]` collapses interleave.** Claude's reply naturally alternates text → tool → text → tool. Earlier reducer concatenated all text deltas into a single `turn.text` string and stacked all tool blocks into a flat `turn.tools[]`. That rendered correctly when there was at most one of each, but for any non-trivial reply it lost the original "speak, then act, then speak again" rhythm — users saw a wall of concatenated narration followed by an unrelated list of tool cards | "Claude replied something weird — none of this matches the context I see in the terminal" | `ClaudeTurn.blocks: AssistantBlock[]` (tagged union of `{kind:'text',text}` / `{kind:'tool',tool}`), order = jsonl content[] order. Streaming reducer maps content-block index → array position so deltas land in the right slot, jsonl rebuild just iterates the assistant message's content[]. Renderer becomes `blocks.map(b => b.kind === 'text' ? <Markdown> : <ToolCall>)`. `joinAssistantText(blocks)` helper recovers a flat string when needed (test asserts, copy-to-clipboard, etc.) |
| **PreToolUse hook fires even when `--dangerously-skip-permissions` is on**, because that flag silences the CLI's BUILT-IN prompt but not our Bridge HTTP hook (the hook is registered in `~/.claude/settings.json` and Claude calls it regardless of the bypass flag). If you take the user's "bypass" choice seriously, you have to **also** short-circuit the dispatcher | User picked bypass-permissions to avoid getting prompted, but our Allow/Deny card keeps popping anyway — confusing, especially since the CLI itself stays quiet | Dispatcher gets an `isBypassSession` callback (`mgr.GetClaudeBypass`) alongside `isRecapSession`. Bypass session + non-AskUserQuestion → `autoAllow`. AskUserQuestion still routes to the WS subscriber (it's a question for the user, auto-allowing it strands the question). See `internal/claude/dispatcher.go::OnAsk` |
| **JSON tag direction matters: payload structs in `internal/claudestate/events.go` deserialize wire formats from `internal/claude` (stream-json + hooks), which are `snake_case` (`tool_use_id`, `total_cost_usd`, `input_tokens`). If you write them with camelCase tags ("for consistency with the frontend"), `json.Unmarshal` silently produces zero-valued payloads** and every snapshot stores `0 in → 0 out` and `totalCostUsd: null` forever. The bug is invisible to unit tests that fixture payloads via Go struct literals, because struct literals bypass the JSON wire round-trip | Live UI shows correct cost/usage during streaming (server pushes raw stream-json), but `/claude-state` returns 0 for every field on refresh. Snapshot file on disk also shows 0 | Payload types whose source is `internal/claude` use snake_case JSON tags (`ToolUseStartPayload.ToolUseID json:"tool_use_id"`, `ResultPayload.IsError json:"is_error"`, `MessageDeltaUsage.InputTokens json:"input_tokens"`). Server-originated frames (TurnStarted, ToolDecisionApplied, ClaudeError) stay camelCase since the frontend reducer reads them as such. Regression tests `TestApply_*DecodesSnakeCaseWire` drive an actual JSON wire byte slice through `Event.UnmarshalJSON` instead of going straight to the typed payload struct — that's the only way to catch this class of bug |
| **`nil` slice in Go marshals to JSON `null`, not `[]`.** Frontend reducers dereference `.length` / `.keys` on collection fields without null guards (`sessionStatus.ts:34` checks `c.pending.length > 0`). If `DeepCopy` produces a nil `Pending`/`PendingQuestions`/`BgTasks`/`Subagents` because it used `append([]T(nil), src...)` with a nil source, the wire is `"pending": null` and the page crashes at startup with `Cannot read properties of null (reading 'length')`. Same for `finalizeInFlight` setting `state.Pending = nil` directly | First page load after a `finalizeInFlight` (server restart, runner death, error finalization) — WorkspacePage throws and unmounts | `ClaudeState.DeepCopy` uses `make([]T, len)` + `copy()` for slice fields and `make(map[K]V)` for maps, guaranteeing non-nil even for empty source. Reducers that clear collections use `s.state.Pending = s.state.Pending[:0]` then defensively re-allocate `[]T{}` if the underlying array was nil. Regression test `TestClaudeState_DeepCopy_CollectionsNeverNil` |
| **Server-as-truth means in-flight turns are zombies after restart.** The Claude CLI runner lives in process memory only. When server (or the runner subprocess) dies mid-turn, the snapshot persists `turns[-1].done=false` but no events will ever arrive to mark it done. Without intervention, frontend hydrates the half-finished turn and spins forever on "Claude is thinking..." with a timer that never stops; composer stays disabled because `inFlight=true`. **Worse**: `claudehistory.Parse` force-sets `done=true` on every jsonl turn regardless of whether result fired, so reading `done` from the merged Turn (jsonl-side optimism) hides the zombie | After a graceful restart or kill -9, the previously-running turn shows spinner forever; user can't send the next prompt | `Loader.Load` reads staleness from the SNAPSHOT (`snap.Turns[-1].Done`, not the merged result) BEFORE merge, then unconditionally finalizes the trailing turn POST-merge if stale: `Done=true, IsError=true, FinishedAt=now`, append text block "Server restarted while this turn was running...". Frontend's existing error-turn styling + `useElapsed`'s `finishedAt` cutoff then surface the dead state immediately. Test: `TestLoad_StaleTrailingTurn_FinalizedDespiteJsonlDoneTrue`. Pair with the WS-reconnect rehydrate in `useSessions` so a reconnect (not just a hard reload) also flushes the zombie |
| **WS reconnect alone does NOT re-hydrate ClaudeState.** WS `ShellSocket` auto-reconnects with backoff, so the connection state transitions back to `'open'` after a brief blip — but `useClaudeStateLoader` is guarded by `turnsLoaded=true` and won't re-fetch. The in-memory ClaudeState is whatever it was when the WS dropped (typically: a stale in-flight turn) | Server restarts in dev; WS reconnects in 1s; user thinks everything's fine but the turn never finishes because the frontend's view of the turn is from before the restart | `useSessions` tracks `wsEpoch` (incremented on every reconnect transition non-`'open'` → `'open'`, guarded by `prev !== 'connecting'` so the first connect doesn't count) and each `useClaudeStateLoader` hydrate stamps `claude.hydrateEpoch = wsEpoch`. A second effect watches `selectedSessionID + wsEpoch` and clears `turnsLoaded` whenever the active session's `hydrateEpoch` trails the current epoch. Background sessions (unselected at reconnect time) stay stale until visited, then refetch lazily on selection — the loader's `turnsLoaded` check picks them up. This replaced an earlier "refetch every session at once" approach that would N-fan-out on every reconnect. Pair with `claude-chat__disconnected` banner so the user sees WHY their state just refreshed |
| **The reaper goroutine wrote a `claude_run_ended` frame BUT NEVER called Apply.** Plus `runClientLoop`'s defer `takeAll()` emptied the runStates map before the reaper got there — so on WS disconnect, the reaper's own `take()` returned `owned=false` and the entire run_ended branch was skipped. Net effect: WS drops mid-stream → runner SIGINT'd → process exits → snapshot persists `Done=false` / `InFlight=true` forever → next reconnect's `/claude-state` hydrate still shows "Claude is thinking…" with no way out (only `finalizeStaleTrailingTurn` at the next server boot recovers it). This violated **invariant #4**: a terminator that doesn't go through Apply is invisible to server state | "I closed the laptop for 30 seconds. Reopened it. Claude is still 'thinking' from before. Sending a new prompt does nothing — composer is locked. Page reload doesn't help. Only restarting the server fixes it." | Split into `applyClaudeRunEnded(sid, reason, mgr)` (Apply only, used by no-frame paths) and `dispatchClaudeRunEnded` (Apply + emit, used by reaper while client is live). Every terminator path is wired: reaper happy-path uses dispatch; `runClientLoop` defer iterates `takeAll()` (which now returns sessionID-tagged entries) and calls `applyClaudeRunEnded` per entry; `closedCh` and `exit_claude` also call apply. Verified end-to-end by a smoke script (`/tmp/smoke_p0b.sh`-style) that opens WS, sends a prompt, slams the socket mid-stream, then asserts `InFlight=false` on a fresh REST hydrate |
| **Spawn-failed turns sat optimistic-InFlight forever.** `handleClaudePrompt` calls `dispatchClaudePromptBegin` (BeginTurn, sets `InFlight=true`) BEFORE forking `claude -p`. When `runner.Prompt` failed (binary missing, OOM, fork limits hit), the old code emitted only a plain `error` frame — Apply was never called, so the optimistic turn stayed `Done=false` and the composer locked. Same invariant #4 violation as the reaper bug | "Sent a prompt, got a red error toast, but the composer never unlocked. Have to refresh the page" | `dispatchClaudeError(sid, code, message, mgr, write)` synthesizes a `ClaudeError` event and routes it through Apply (the reducer treats it as a `finalizeInFlight` trigger), then emits the `claude_error` frame to the client. handleClaudePrompt's spawn-failed branch uses it. Test: `TestWS_ClaudeError_FinalizesInFlightTurn` |
| **WS WriteJSON failures were silently dropped, leaving the runner blocked forever pushing onto a 64-slot `claudeEvents` channel nobody was draining.** A half-closed TCP socket would still report writable to gorilla/websocket's Write loop and return errors that callers did `_ = write(...)`. The runner goroutine kept pumping events into the channel, filled it, then blocked on send — `pr.Events` never closed → reaper never woke → process kept running, kept burning tokens, and InFlight stayed true on a session whose human had walked away. The dead conn could sit for minutes before the OS detected it | Browser crashes / kill -9 on the tab → server doesn't notice for ages; meanwhile Claude finishes writing a 30k-token reply nobody will read. Discovered when tokens-per-day didn't match active sessions | `guardedWriter(writeJSON, close)` returns a serialized write closure that closes the conn on FIRST write failure → reader's ReadJSON errors → `close(inbound)` → main select returns → deferred `claudeRunStates.takeAll() + stopRun()` SIGINTs the runner → forwarder unblocks via `<-ctx.Done()` → reaper unblocks → through invariant #4 the state finalizes. Subsequent writes short-circuit with `errClientGone`. Tests: `TestGuardedWriter_FirstFailureClosesConnAndShortCircuits`, `TestGuardedWriter_SerializesConcurrentWrites` |
| **Session DELETE didn't propagate to `claudestate.SessionManager` — entries + Persister goroutines leaked.** `session.Manager.Close` did the user-visible work (kill shell, delete store dir, fire close listener) but `claudestate.SessionManager.sessions[sid]` stayed, the Persister kept holding a flock on `<dataDir>/sessions/<sid>/claude.json` (a file in a directory that no longer exists), and `GetOrLoad` for the same sid would return the stale `*SessionState`. Worse: the Persister's eventual `Close`-time `writeSnapshot` failed with ENOENT and logged WARN "snapshot write failed" — misleading noise that hid real errors | Long-running pod: deleted sessions accumulate in memory; flock-related warns clutter logs; theoretically (if a new session reused an old sid, which ulid prevents) GetOrLoad could return a Persister flock'd to a deleted file | `Persister.CloseNoFlush` + `SessionState.CloseNoFlush` skip the final write (the dir is gone — there's nothing to write); `claudestate.SessionManager.DeleteSession(ctx, sid)` uses CloseNoFlush. main.go's `mgr.AddCloseListener` calls into it. Tests: `TestSessionManager_DeleteSession_FreesEntry`, `TestPersister_CloseNoFlush_SkipsWriteAndIgnoresMissingDir` |
| **`applyResult` didn't check `turn.Done` — a late `result` event could resurrect a turn that finalizeInFlight had already closed.** Happens when graceful shutdown's `FinalizeAllInFlight` marks turns Done+IsError, then the runner emits its final `result` event during srv.Shutdown's HTTP drain window, then `dispatchClaudeStreamEvent` routes it through Apply, then `applyResult` overwrites IsError back to false / replaces FinishedAt / sets a TotalCostUsd field. The snapshot ends up self-contradictory: `LastError={code:"server_shutdown",...}` next to a turn marked "completed successfully" with a cost. The frontend renders the turn as success but the global error banner says shutdown — the user is told two opposite things | Wouldn't reliably reproduce locally — only emerges under SIGTERM + a Claude turn near completion. Detected by adversarial review of the FinalizeAllInFlight commit before ship | `applyResult` short-circuits on `turn.Done`: "the earlier terminator wins; we just leave InFlight=false in place." Test: `TestApply_Result_AfterTerminator_DoesNotOverwriteDone`. General rule for all per-event reducers: if the event would mutate a Done turn, return early — terminator order may be racey but the *earlier* one is authoritative |
| **`Event.UnmarshalJSON`'s default branch returned an error for unknown event kinds** (notably stream-json's `system` and `rate_limit` metadata frames). `dispatchClaudeStreamEvent` would then log a WARN and fall through to a raw passthrough frame — harmless to clients (these events are no-ops) but produced dozens of WARN lines per turn that drowned out real errors in `kubectl logs` | "Did the server have an error? Hard to tell, the log is wall-to-wall `unknown event kind \"system\"`" | Declare `EventSystem` / `EventRateLimit` alongside `EventMessageStart` / `EventMessageStop` etc. in the no-payload branch of `Event.UnmarshalJSON`. They still no-op in the reducer; they just don't error out at the dispatch boundary anymore |
| Claude CLI is the SOLE owner of `Bash(run_in_background)` lifecycle. It parents them as children (not detached via `setsid`) and SIGKILLs every in-flight task when `claude -p` exits, emitting `task_updated.patch.status="killed"`. alfred is a pure observer. | A future engineer adds a "Stop bg task" button or a tmux-foster scheme; CLI's own state diverges and re-emits stale events; users see duplicate "killed" events or stalled UIs | `TestApplyTaskUpdated_KilledSetsStatusAndSummary` + `DESIGN.md` ADR-001 |
| CLAUDE_CODE_TMPDIR is hardcoded to `/tmp/claude-<uid>` if unset (NOT `os.TempDir()`, NOT `$TMPDIR`). On a Kubernetes node sharing `/tmp` across pods of the same uid, two alfred pods would silently share the bg-task scratch root, causing the second pod's ownership check (mode 0700) to fail. alfred-server's bootstrap sets `CLAUDE_CODE_TMPDIR` to `<dataDir>/claude-tmp` to namespace. | The first deploy after a `runAsUser` change shows "permission denied" on `mkdir /tmp/claude-1000/...` from claude -p, but the message disappears mid-stream-json and the only symptom is empty bg-task log files | `TestBootstrap_OperatorOverride` + `internal/claudebgtasks/env.go::Bootstrap` + DESIGN.md ADR-007 |
| BgTask `Status` enum has 5 values: `in_progress`, `completed`, `failed`, `killed`, `stopped`. Adding a 6th means updating the Go reducer (`applyTaskUpdated` guard), the TS reducer, the visual map in `BackgroundTasksPanel`, AND the Loader's "force in_progress to killed" rule. | Adding e.g. `paused` and only updating the Go side; the panel chip renders unstyled (no CSS class for unknown status) and Loader still force-kills it | `TestApplyTaskUpdated_KilledSetsStatusAndSummary` + visual inspection of `BackgroundTasksPanel.tsx` status map |
| BgTasks are re-derived on `Loader.Load` by replaying jsonl `task_started`/`notification`/`updated` events; any task still `in_progress` after replay is FORCED to `killed` with summary "killed when server restarted". Subagents are NOT replayed (ADR-009 — they require a live main Claude, which is dead after restart). | A future engineer "fixes" the force-kill to preserve `in_progress` for "accuracy"; the panel lies, showing dead tasks as live | `TestLoader_RebuildBgTasksFromJsonl_MarksInProgressAsKilled` + `TestLoader_RebuildBgTasks_HonorsSubagentsNoRebuildRule` |

---

## Layering / module boundaries

```
api/ (HTTP + WS — the only module that knows about all three below)
 ├── shell/   (bash + PTY + parser + broadcaster) — knows nothing about HTTP, users, storage
 ├── store/   (JSON file persistence)              — knows nothing about shell or users
 └── auth/    (credentials + rate limiter)         — knows nothing about shell or storage
static/      (embed.FS of web/dist with SPA fallback)
```

`shell` does not import `store`. The output buffer is handed to `api` via `EndedEvent.Output` and `api` calls `store.WriteOutput`. If you find yourself adding a `store.Store` parameter to `shell`, push back — keep the layering.

---

## What's intentionally NOT in the codebase

- No JWT / session store / refresh tokens.
- No user management / SSO / RBAC.
- No multi-session (per-user) tabs. The schema reserves room for a `session_id` field if you later need it.
- No PTY emulation. `vim` / `top` / `htop` will produce ANSI garbage; they're explicitly out of scope.
- No request-body / header logging. Commands may contain secrets in env vars; tokens are never logged.
- No automatic restart of interrupted commands at boot. The sweep marks them `interrupted` so the UI shows the truth; replay is unsafe (think `git push`).

---

## Provenance

- Brainstorm + spec: `docs/superpowers/specs/2026-06-11-headless-alfred-design.md`
- Implementation plans (the actual build journey, with code blocks for every step): `docs/superpowers/plans/2026-06-11-headless-alfred-*.md`
- Built over the course of 2026-06-11 via the superpowers brainstorm → spec → plan → implement loop. Real bugs discovered during integration and E2E are catalogued in the README's commit log.

---

## Release flow

`main` = production (push triggers oracle deploy via CI).
`next` = long-lived wip branch (no CI deploy).

Daily work happens on `next`. When `next` has enough to ship as a
new version, merge `next` → `main`, bump `Chart.yaml` appVersion
(manual semver: v0.1, v0.2…), add a `docs/RELEASES.md` entry, tag
the commit, push tag + main. CI takes over.

Full procedure + rollback playbook in `RELEASE.md` at the repo
root. User-facing changelog in `docs/RELEASES.md`.

---

## Quick orientation for "I want to change X"

| Change | Where to look first |
|---|---|
| The shell behavior (Stop, restart, buffer cap) | `internal/shell/shell.go` |
| How commands are framed in/out of bash | `internal/shell/sentinel.go` (and the `Wrap` function) |
| The HTTP API surface | `internal/api/{router,login,commands}.go` |
| WS protocol | Schema in `internal/api/wsproto.go`. Server: shell-mode handlers + main connection loop in `internal/api/ws.go`; Claude-UI handlers in `internal/api/claude_handlers.go`. Client: `web/src/lib/ws.ts` (typed unions for `ServerMsg` / `ClientMsg`) |
| Frontend UI (chat stream) | `web/src/features/terminal/ChatStream.tsx` (UserBubble + AssistantBlock pairs); `web/src/features/sessions/useSessions.ts` owns the hook. Per-session state mutations are split: shell-mode lifecycle (`idle` / `reattach` / `started` / `chunk` / `done`) in `sessionsReducer.ts`; Claude-UI lifecycle (`claude_entered` / `claude_exited` / `claude_event` / `tool_approval_request` / `claude_error` / `claude_run_ended`) in `claudeReducer.ts`. `reducePerSession` delegates to `reduceClaudeMsg` first; null = fall through to the shell switch |
| What's stored to disk | `internal/store/store.go` + `internal/store/record.go` |
| Container build | `Dockerfile` + `scripts/build-image.sh` |
| K8s manifests | `deploy/manifests/` |
| Cluster prerequisites + ops runbook | `deploy/README.md` |
| Add a new tmux operation | `internal/shell/tmuxio/runner.go` (also add to `FakeRunner`) |
| Modify session lifecycle (create, close, reconcile) | `internal/session/manager.go`; lifecycle listeners (`onCreate` / `onClose` / `onRename`) use the generic `listenerSet[T]` in `internal/session/listeners.go` |
| Add a new server-pushed WS frame type | `internal/api/wsproto.go` (add field to `OutMsg`) + emit from `internal/api/ws.go` (shell-mode frames) or `internal/api/claude_handlers.go` (Claude UI frames) + `web/src/lib/ws.ts` (add to `ServerMsg` union) + handle in `sessionsReducer.ts` (shell) or `claudeReducer.ts` (Claude) |
| Change bash init env (echo, prompt, readline opts) | `internal/shell/tmux_shell.go` — `configurePane()` is the single source of truth, called from both `Start` and `Stop`'s respawn path |
| Change the sidebar UI | `web/src/features/sessions/SessionsSidebar.tsx`. Layout from top: collapse `«` + `+ New chat` (same row), `Recap` button, ACTIVE SESSIONS header, scrollable session list, **footer with Git credentials / Claude credentials / Sign out**. Per-row turn indicator dot is supplied via the optional `statusForSession` prop (so isolated tests don't need to wire it). Auth buttons in the footer are optional too — if `onLogout`/`onOpenGitCredentials`/`onOpenClaudeCredentials` aren't passed, the footer doesn't render |
| Workspace 3-column layout (left, main, right) | `WorkspacePage.tsx`'s `gridTemplateColumns` is computed inline from `useResizableWidth({ storageKey, initial, min, max, edge, archiveThreshold?, onArchive? })` for each side. Drag handles are absolute-positioned strips inside `.workspace__left-pane` / `.workspace__right-pane`. Drag-below-threshold auto-archives (left → `setLeftCollapsedPersisted(true)`, right → `setSidebarHiddenPersisted(true)`); the hook resets width to `min` on archive so reopen never starts as a sliver. Left collapse: separate `leftCollapsed` state + 40px rail with `»` expand button. The right pane renders `RightRail` (Summary + Notes accordion) for chat sessions XOR `RecapSidebar` for recap sessions. Widths persist in `localStorage['alfred_left_sidebar_width' / 'alfred_right_sidebar_width' / 'alfred_left_sidebar_collapsed' / 'alfred_right_sidebar_hidden']` (the last was migrated from the older `alfred_summary_sidebar_hidden`; sessionsReducer still reads the old key as fallback for one release cycle). Reopen the right sidebar via the header toggle icon (`workspace__sidebar-icon-btn`); the left, via the `»` rail button or the `«` button inside the sidebar's top row. No close × buttons on the sidebars themselves. `.workspace` itself is `grid-template-rows: 100vh` + `overflow: hidden` so a long chat can never overflow the viewport (see the `minmax(0, 1fr)` trap above) |
| Git auth UI / credential storage | `web/src/features/sessions/GitCredentialsDialog.tsx` (form) + `web/src/lib/api.ts:saveGitCredentials` (client) + `internal/api/git_credentials.go` (writes `~/.git-credentials` + `~/.gitconfig` `credential.helper=store`) |
| Claude auth UI / OAuth credential storage | `web/src/features/sessions/ClaudeCredentialsDialog.tsx` (textarea form) + `web/src/lib/api.ts:saveAnthropicCredentials` (client) + `internal/api/anthropic_credentials.go` (writes `~/.claude/.credentials.json` mode 0600 via tmp+rename). The canonical path was empirically determined by probing — claude 2.1.x reads **only** the leading-dot file `~/.claude/.credentials.json`, no other candidate works. |
| ANSI color rendering in chat output | `web/src/features/terminal/ChatStream.tsx` — `renderAnsi()` via `ansi-to-html` with `escapeXML:true`. Add new SGR codes by extending the `colors:` palette in the `AnsiToHtml` constructor |
| Validate/reject a command shape before it reaches bash | `internal/api/git_commit_guard.go` is the template — wired into `handleRun` in `internal/api/ws.go` (which `handleInbound` dispatches `case "run"` to). Useful for any command that would hang because the container doesn't ship an interactive program |
| Claude-mode terminal (xterm.js bytes ↔ PTY stdin) | `web/src/features/claude/ClaudeTerminal.tsx` (frontend), `internal/shell/tmux_shell.go` (`EnterClaude` / `SendStdin` / `ExitClaude`), `internal/api/claude_handlers.go` (`handleEnterClaude` / `handleStdin` — shared with UI mode, dispatch by renderer; `pty_data` outbound emitted by the main loop in `ws.go`). The mode flag persists in `sessions.json` per `store.SessionMode`; reset on Pod restart in `Manager.Reconcile`. Design spec: `docs/superpowers/specs/2026-06-13-claude-mode-design.md` |
| Claude UI mode (ChatGPT-style rendered chat) | Backend: `internal/claude/{runner,parser,event,bridge,dispatcher,settings}.go` — forks `claude -p` per prompt, parses stream-json (including the `assistant` snapshot to recover the assembled tool input — see `parseAssistantToolInputs`), routes tool approvals through a localhost HTTP bridge. WS protocol: `claude_event` / `tool_approval_request` / `claude_error` / `claude_run_ended` outbound, `claude_prompt` / `tool_decision` / `interrupt` inbound. All Claude-UI WS handlers live in `internal/api/claude_handlers.go`; see `handle{ClaudePrompt,ToolDecision,EnterClaude,ExitClaude}` and the mutex-guarded `claudeRunStateMap`. Frontend: `web/src/features/sessions/ClaudeChatView.tsx` (chat scroll + markdown via react-markdown + remark-gfm + Prism syntax highlight in fenced code blocks), `StartClaudeDialog.tsx` (renderer picker), `ToolApprovalCard.tsx` (Allow / Deny for normal tools), `AskUserQuestionCard.tsx` (Claude's AskUserQuestion tool — renders radio/multi-select + Other, answer rides back via `tool_decision('deny', reason)` so the CLI surfaces it as the tool_result). State: `ClaudeState` / `ClaudeTurn` / `ClaudeQuestionRequest` in `types.ts`, reducer in `claudeReducer.ts`. End-to-end smoke: `scripts/claude-ui-smoke`; Playwright regressions in `web/e2e/regression.spec.ts`. Design spec: `docs/superpowers/specs/2026-06-13-claude-ui-mode-design.md` |
| Claude UI chat history restore (jsonl-derived) | Backend: `internal/claudehistory/{types,locate,parse}.go` — `Locator.Locate(sid, uuid)` walks `~/.claude/projects` for `<uuid>.jsonl` (cached per sid, invalidates if path is unlinked); `Parse(path, limit, before)` builds `[]Turn` from `user` (string content → new turn), `user` (tool_result → resolve), `assistant` (text → append text; tool_use → append tool). Unknown line types silently skipped. Handler: `internal/api/claude_history_handler.go::GetClaudeHistoryHandler` (returns `[]` for empty/missing, never 404 for "no history"). Frontend: `web/src/lib/api.ts::getClaudeHistory`, `web/src/features/sessions/useClaudeHistoryLoader.ts` (one-shot per page, preserves local turns when fetch returns empty to avoid race wiping in-flight conversation). Mounted in `WorkspacePage`. Design spec: `docs/superpowers/specs/2026-06-15-claude-history-restore-design.md` |
| Per-session summary template | Backend: `internal/template/{builtin,render}.go` — `summary-todo` template asks Claude to maintain `<DATA_DIR>/summaries/<sid>.md`. Auto-allow in `internal/claude/dispatcher_summary.go` (8KB cap). Watcher in `internal/summary/watcher.go` emits `summary_updated` WS frames per-session. Frontend: `SummarySidebar.tsx` is now the **`SummarySection`** content component (no wrapper) consumed by `RightRail.tsx` as one accordion section. Toggle the whole rail via the `.workspace__sidebar-icon-btn` SVG icon in the header — state persists in `localStorage['alfred_right_sidebar_hidden']` (migrated from `alfred_summary_sidebar_hidden`). Summary section is gated by `selected.kind !== 'recap' && ps.templateId === 'summary-todo'`; rail itself shows whenever `!isRecap` (Notes always renders). `templateId` is rehydrated from the WS `idle`/`reattach` frames (server reads `SessionMeta.TemplateID`) so a page reload doesn't lose it. Design spec: `docs/superpowers/specs/2026-06-15-summary-template-design.md` |
| Per-session Notes (user-only scratchpad, not sent to Claude) | Backend mirrors `internal/summary`: `internal/notes/{path,watcher}.go` + `internal/api/notes_handler.go` (GET + PUT, 64KB cap, atomic tmp+rename). `note_updated` WS frame pushed per-WS-subscription same as summary. Frontend: `NotesPanel.tsx` with 600ms debounced autosave + focus-guarded refetch (in-flight typing is never clobbered by a server echo). Mounted in `RightRail.tsx` accordion alongside Summary with `key={sessionID}` so switching sessions remounts the panel (otherwise the trailing debounced PUT would write the OLD text to the NEW session). Notes are deliberately NOT passed through `composePromptText` — Claude never sees them. |
| Daily recap (singleton ephemeral recap session) | Backend: `store.SessionKind` distinguishes `KindChat` from `KindRecap`. `internal/session/manager.go::CreateOrGetRecapSession` is the find-or-create singleton (kills orphans by `Close`). `internal/recap/{path,watcher}.go` for `<DATA_DIR>/recaps/<YYYY-MM-DD>.md` storage + fsnotify watcher. Auto-allow in `internal/claude/dispatcher_recap.go` (16KB cap, strict date-basename regex). HTTP: `internal/api/recap_handlers.go` (4 endpoints: POST/DELETE recap-sessions, GET /api/recaps, GET /api/recaps/{date}); single-session metadata via `GET /api/sessions/{id}` so the chat-filtered list endpoint doesn't have to expose recap sessions. WS: `recap_updated` broadcast via `internal/api/recap_broadcast.go::recapBroadcaster`. Runner: `RunOptions.Continue` → `claude -c` (recap sessions use this so the conversation continues across alfred-server restarts without UUID tracking). **Recap is a backend singleton — never auto-deleted on switch-away.** Earlier versions DELETEd the recap session whenever the user picked another sidebar entry; that broke continuity (next "Recap" click got a fresh ClaudeSessionID, so the prior conversation was unreachable). Now `useSessions` has no auto-delete `useEffect`; `CreateOrGetRecapSession` is what makes "Recap" act like opening a destination. The session lives in `sessions.json` (with `Kind: KindRecap`), tmux pane and uuid intact, until alfred-server restarts. Side effect: `enter_claude` is now idempotent when the renderer matches — re-clicking Recap on a session that's already in claude mode emits a fresh `claude_entered` instead of the red `already_in_claude` error banner. `filepath.EvalSymlinks(recapDir)` is also required before passing cwd to the runner because macOS `/tmp` → `/private/tmp`; the CLI resolves via realpath, but `transcriptExists` was comparing the unresolved path and picking `--session-id` when it should have been `--resume`, hence "Session ID already in use" on every prompt past the first.

**Prompt transparency**: any time the server composes the actual prompt body before piping to `claude -p` (template injection, recap-daily render, summary-todo wrap), it ALSO emits a `user_prompt` WS frame carrying the final text. `claudeReducer.user_prompt` stashes it on the last turn as `expandedPrompt`; `UserPromptBubble` adds a "Show full prompt sent to Claude (N chars)" toggle when `expandedPrompt !== prompt`. The toggle is hidden for plain turns where the visible bubble already equals what was sent. This is the user's audit trail for token consumption — without it they see "Say hi only" in the chat and get billed for 800+ injected tokens.

Generate flow: the RecapSidebar's "Generate" button sends `claude_prompt` with empty text + `renderTemplate: 'recap-daily'`; `handleClaudePrompt` server-side calls `template.Render` with the absolute `recap.Path(dataDir, today)`. The frontend owns NO placeholder resolution. **Recap sessions ALSO spawn claude with `cwd = <DATA_DIR>/recaps/`** (via `isRecapKind(m, sid)`), not `claudeInvocationCWD()` — this gives Claude `ls`-visible access to all past recaps as neighbor files and scopes claude-mem / file-history to just the recap dir, not the user's whole `$HOME`. The recap-daily template is written assuming this cwd (e.g. "ls + Read recent files for continuity"); changing the cwd without updating the template will make Claude look in the wrong place. Recap sessions also opt out of the tool-approval card entirely via a dedicated `isRecapSession` predicate in `Dispatcher.OnAsk` — bypassPermissions only silences the CLI's own prompt; without the predicate, Bash/Read/Write would still pop approval cards and (in headless tests, or for a distracted user) freeze the run. **AskUserQuestion is the exception** — it's a question for the human, not a tool call, and must reach the frontend so AskUserQuestionCard can render the radio/multi-select UI. The recap fast-path explicitly skips `ToolName == "AskUserQuestion"`. Frontend: `Recap` button in `SessionsSidebar.tsx`, `useSessions::createOrEnterRecap` + auto-delete on switch-away via `prevRecapIdRef`. `RecapSidebar.tsx` (right rail with date list + generate button + content; mutually exclusive with SummarySidebar). `MarkdownView.tsx` is the shared renderer (extracted from SummarySidebar for DRY). `recapFetchCounter` lives at the top of `useSessions` state (NOT per-session), because recap files are global. The Generate button renders the `recap-daily` template client-side (fetched via existing `GET /api/templates/{id}`) with `<cwd>` substituted as `$(pwd)` for Claude to evaluate. Design spec: `docs/superpowers/specs/2026-06-15-daily-recap-design.md` |
| IME-aware composer Enter | `web/src/lib/keyboard.ts::isSubmitKey(e)` — single shared helper used by all three composers (`CommandInput.tsx` shell, `ClaudeChatView.tsx` Claude UI textarea, `SessionsSidebar.tsx` rename input). Checks Enter + !shift + `!e.nativeEvent.isComposing`. Don't write inline `e.key === 'Enter' && !e.shiftKey` — it ships half-finished IME commits. Design spec: `docs/superpowers/specs/2026-06-15-ime-aware-composer-enter-design.md` |
| Session indicator (the single dot per session) | `web/src/features/sessions/sessionStatus.ts::sessionIndicator(connState, ps)` folds (connection state, in-flight, pending user decision) into one enum: `'idle' | 'busy' | 'needsAction' | 'disconnected'`. Rendered by `SessionIndicatorDot.tsx` — green dot / red dot / yellow pulsing dot / warning-triangle SVG. Used in the workspace header (next to brand) and in every SessionsSidebar row. Priorities: `disconnected > needsAction > busy > idle` — needsAction wins over busy because Claude `inFlight` is usually still true while a tool-approval / AskUserQuestion card sits there, but the user's decision is what unblocks Claude; surface the actionable state. Shell mode treats `running != null` as busy so the semantic is consistent across modes. WS not open ⇒ disconnected overrides everything (we don't actually know what Claude is doing if we can't talk to backend) |
| **Dual-frontend dev mode** (local Vite HMR talking to deployed backend) | `web/vite.config.ts` reads `VITE_BACKEND_URL`. Default points at local backend (`http://localhost:8080`); pointing it at the deployed pod via the ssh tunnel (`VITE_BACKEND_URL=http://alfred.local:8888 npm run dev`) gives you Vite HMR on the frontend while every `/api` and `/ws` request rides the proxy through to the production-deployed backend (same `/data` PVC, same Claude state, same WS). Two URLs share the same backend: `http://localhost:5173/` (your dev frontend) and `http://alfred.local:8888/` (the embedded prod dist served by the pod). localStorage tokens are per-origin so each URL needs its own login. Don't forget: any state you mutate via 5173 is real — sessions you create show up on 8888 too |
| Assistant reply timeline | `ClaudeTurn.blocks: AssistantBlock[]` is the source of truth — text and tool blocks interleaved in jsonl-recorded order. Stream reducer (`claudeReducer.ts` `text_delta` / `tool_use_start` cases) tracks `_blockIndexMap` so each delta lands at the right index; jsonl rebuild (`internal/claudehistory/parse.go::handleAssistant`) iterates assistant message content[] preserving order. Renderer `blocks.map` dispatches on `block.kind`. Use `joinAssistantText(blocks)` when you need a flat string (tests, clipboard, summary previews) |
| Extended-thinking blocks | `ClaudeTurn.thinking?: string[]` (separate from `blocks` because thinking is internal reasoning, not part of the visible reply). Stream side: `KindThinkingDelta` event (parser sees `content_block_delta.type === 'thinking_delta'`, reads `delta.thinking`). jsonl side: `handleAssistant` recognises `content[].type === 'thinking'`. Renders as `ThinkingBlockView` — default-collapsed cards above the blocks timeline, expand to read with full `AssistantMarkdown` (Prism + tables). **Won't fire** unless Claude is configured to emit thinking — currently nothing in Alfred sets `~/.claude/settings.json::alwaysThinkingEnabled: true`, so the rendering is effectively dead until you flip that switch (or wait for a model that thinks by default) |
| Claude session cwd | `internal/api/claude_handlers.go::handleClaudePrompt` calls `sh.CurrentCWD()` (= `tmux display-message -p '#{pane_current_path}'`) and passes it as `claude -p`'s working dir, with `filepath.EvalSymlinks` to dodge the macOS /tmp realpath mismatch. Falls back to `claudeInvocationCWD()` (`/home/alfred` or `$HOME`) if tmux returns empty. Recap sessions still pin to `<DATA_DIR>/recaps/` regardless. Means `ls`, `Read`, `Write`, `Glob` etc all behave like the shell session the user just left |
| Header layout (brand+dots / Claude / sidebar-toggle) | `WorkspacePage.tsx` `.workspace__header` is a **grid** `1fr auto 1fr` with explicit `__header-left / __header-center / __header-right` slot divs. Do NOT use flex + two `margin-left:auto` — that puts both items in the center (see traps table). The `--exit` modifier on the Claude button uses red border because Exit Claude is destructive (kills bash + cwd) |
| Markdown file path display (summary/notes) | Backend `internal/api/markdownfs.go::serveMarkdownFile` emits `X-File-Path` header on **both** 200 and 404-not-found (UI needs to show the path before the file is created). Traversal-rejection 404 deliberately does NOT emit the header (would confirm path existence outside root). Frontend `lib/api.ts::fetchMarkdownDoc` returns `{text, path}`; `PathStrip.tsx` renders the absolute path + Copy button. RTL `text-overflow: ellipsis` truncates the **left** so the identifying sid tail stays visible |
| Reusable document-sync hook | `web/src/lib/useDocumentSync.ts` — `(fetcher, deps, options) => {data, error, firstFetchPending}`. Used by `SummarySection` and `NotesPanel`. Single `skipIf` option, used by `NotesPanel` to refuse server-echo overwrites while textarea is focused. NOT a fit for `RecapSidebar`'s content fetch (that one shows a spinner on EVERY refetch; this hook is "spinner once, silent refetches"). When designing reusable hooks, encode the hook's policy in its doc comment and **state what it doesn't fit** so future contributors don't keep retrofitting knobs |
| Generic fsnotify watcher | `internal/diskwatcher/watcher.go` — `Start[K comparable](dir, parseFilename, debounce, onWrite, logName)`. Used by `internal/{summary,notes,recap}/watcher.go` as thin wrappers (each just provides a regex + key extractor). Stop order is `close(stop) → <-done → close(fsnotify)` — drains pending events before tearing down the source |
| Canonical-IO auto-allow predicate (Claude dispatcher) | `internal/claude/canonical_io.go::isCanonicalIO(req, maxBytes, matchPath)` — shared scaffold for Read/Write auto-allow against a fixed canonical path. Match policy is a callback so summary (exact equality, no Clean) and recap (Clean + dir + basename regex) keep their distinct security boundaries. **Do not unify them into "always Clean first"** — that loosens summary's strict equality (which deliberately rejects `/data/summaries/./X.md`) |
| Sidebar layout (collapse button + footer) | The collapse `«` button lives INSIDE `SessionsSidebar.tsx` (`.sessions-sidebar__top-row` flex-row with `+ New chat`), not on `.workspace__left-pane` — so it shares the sidebar's `--surface` background instead of floating on the pane's darker `--bg`. The footer (`Git credentials` / `Claude credentials` / `Sign out`) is at the bottom; the session list above uses `flex: 1 + min-height: 0 + overflow-y: auto` so it scrolls internally and the footer stays pinned. Collapsed (40px) strip uses `.workspace__left-collapsed-footer` for the three icon-only buttons |
| Instant CSS tooltip | `web/src/index.css` `[data-tooltip]:hover::after` — set `data-tooltip="..."` on any element to get a zero-delay hover bubble (native `title` waits ~1s, feels sluggish on small icons). Add `data-tooltip-side="left"` to anchor to the element's left edge (useful when the element is near the right viewport edge). No JS, no portal, no positioning library |
| Claude empty-state hero | `ClaudeChatView.tsx` — when `turns.length === 0 && !inFlight && !pending && !pendingQuestions && !lastError`, render a centered "Headless Alfred" title + subtitle with a narrower (`max-width: 720px`) composer at ~38% viewport height (`.claude-chat--empty` grid). First turn flips back to the normal `1fr auto` layout (composer pins to bottom). No animation — natural conditional render |
| Server-authoritative Claude UI state (the "refresh parity" architecture) | New package `internal/claudestate/` is the source of truth. **Apply is the only mutation entry point** (`SessionState.Apply(ev)` under `sync.RWMutex` write lock); WS inbound events, hook-driven updates, and HTTP-mutating handlers all converge there. `Persister` is one goroutine per session, debounces 100ms, writes via tmp+fsync+rename+parent-dir-fsync, holds a `flock` to prevent two server processes racing on the same snapshot. `Loader.Load` merges snapshot + jsonl per the spec's field-level rules (snapshot wins for extension fields: tool startedAt/finishedAt/decision/bgTaskId, turn finishedAt/cost/usage/isError; jsonl wins for skeleton: prompt, blocks order, text content, tool name/input/result). `SessionManager` is a process singleton with `singleflight.Group` so 100 concurrent `GetOrLoad` calls collapse to one Load. HTTP endpoint `GET /api/sessions/{sid}/claude-state` returns the deep-copied in-memory state — the frontend hydrates exclusively from this, not from WS events. Spec: `docs/superpowers/specs/2026-06-18-claude-refresh-parity-design.md`. Three implementation plans in `docs/superpowers/plans/2026-06-18-refresh-parity-{01,02,03}-*.md`. The old `/claude-history` endpoint stays one release cycle with RFC 9745 `Deprecation: true` / `Sunset: <date>` / `Link: rel="successor-version"` headers — remove in the release after this one |
| In-flight turn termination paths (invariant #4) | Six paths that close a turn server-side, all routed through `claudestate.Apply`: (1) `dispatchClaudeStreamEvent` on `EventResult` — happy path. (2) `dispatchClaudeRunEnded` from the reaper goroutine when runner exits without a result (crash, SIGINT, OOM). (3) `dispatchClaudeError` from `handleClaudePrompt` when `runner.Prompt` fails to spawn — the optimistic turn registered by `dispatchClaudePromptBegin` gets finalized. (4) `applyClaudeRunEnded` from `runClientLoop`'s defer iterating `claudeRunStates.takeAll()` after `guardedWriter`'s first failure closed the conn — kills the runner AND finalizes state (no frame; client is gone). (5) `applyClaudeRunEnded` from the `closedCh` case when the session is deleted while still in flight. (6) `applyClaudeRunEnded` from `exit_claude` when the user explicitly leaves Claude mode. Plus the server-wide finalize on graceful shutdown: `csMgr.FinalizeAllInFlight(reason)` called BEFORE `srv.Shutdown` so the snapshot's next-boot state is the truth, not a stale fallback. **Anything that closes a turn but isn't on this list is a bug** — see the invariant #4 explanation upstream. Verified by `/tmp/smoke_p0a.sh` (spawn fail) and `/tmp/smoke_p0b.sh` (WS drop mid-stream), both asserting `InFlight=false` on a fresh REST hydrate after the close event |
| Snapshot file format and lifecycle | `~/.alfred/sessions/<alfred-id>/claude.json` is the per-session snapshot. Schema: `{version: 1, sessionId, claudeUuid, writtenAt, turns: []ClaudeTurn}`. **Only `turns` is persisted** — everything else in `ClaudeState` is derivable (`inFlight = !turns[-1].done`), transient (`pending`, `pendingQuestions`, `lastError`), or an external-resource reference (`bgTasks` → live OS processes, `subagents` → live hook pairings). Persisting transient or external state would surface stale references after restart. Failure matrix in `Loader.loadSnapshot`: missing → silent fallback to jsonl, corrupt JSON → WARN + jsonl, version mismatch → WARN + jsonl, sessionId mismatch → WARN + jsonl, neither → empty state. **Never** unmarshal via reflect or JSON round-trip — `DeepCopy` is hand-written per struct so it preserves typed nil pointers and `json:"-"` fields (the per-turn `_blockIndexMap` bookkeeping). Writes are atomic: tmp+fsync+rename; orphan tmp from a previous crash is `os.Remove`'d on Persister startup (idempotent — discard the error, no Stat-before-Remove race) |
| Event taxonomy + JSON tag direction | `internal/claudestate/events.go` declares `Event` (tagged union) with custom `MarshalJSON`/`UnmarshalJSON` that dispatches `Payload` to a concrete struct keyed off `Kind`. **Payload structs sourced from `internal/claude` (stream-json + hook events) use snake_case JSON tags** to match that package's wire format — see trap table for the war story. **Server-originated payloads** (`TurnStartedPayload`, `ToolDecisionAppliedPayload`, `ClaudeErrorPayload`, `ClaudeRunEndedPayload`, `ToolDecisionPayload`) **use camelCase** since the frontend reducer consumes them as such via OutMsg fields and the WS protocol mirror in `lib/ws.ts`. `MessageDeltaUsage` is a snake_case wire mirror of `TokenUsage` (camelCase, on-snapshot); `applyMessageDelta` copies field-by-field between them. If you add a new event kind: source it through `Event.UnmarshalJSON`'s switch AND add a kind-dispatch unit test that goes through real JSON bytes (not Go struct literals) — see `TestApply_*DecodesSnakeCaseWire` |
| Server-stamped timestamps on WS frames | `OutMsg.Timestamp` (RFC 3339 nano, UTC) is populated by the three dispatch helpers in `ws.go` (`dispatchClaudeStreamEvent`, `dispatchToolDecision`, `dispatchClaudePromptBegin`) from `time.Now().UTC()` at `Apply` time. Frontend reducer reads it into corresponding state fields (`Turn.startedAt`, `Tool.startedAt`, `Tool.finishedAt`, etc.) so timestamps are bit-identical between live streaming and post-refresh hydrate. **Do not** call `new Date().toISOString()` in the reducer — `asTimestamp(frameTs)` reads from the payload first and only falls back to client time on local-only backstop paths (`beginClaudeTurn`'s optimistic placeholder, `finalizeInFlightTurn` when called WITHOUT an event timestamp). Server's `MessageUsage.TotalCostUSD` snake_case (`total_cost_usd`) decodes into `ResultPayload.TotalCostUsd` — note the unrelated Go-field capitalization difference is intentional and OK because the JSON tag bridges them |
| Optimistic prompt + turn reconciliation | `useSessions.claudePrompt` generates a `clientNonce` (random 16 chars via `randomId()`), calls `beginClaudeTurn(c, label, { clientNonce })` which parks the placeholder turn under id `pending:<nonce>`, then sends `claude_prompt` with `clientNonce` in the outbound frame. Server's `dispatchClaudePromptBegin` allocates the authoritative `turnId` (`ulid.Make()`), calls `BeginTurn(turnId, ...)`, and broadcasts `turn_started { clientNonce, turnId, timestamp }`. `reduceClaudeMsg`'s `turn_started` case swaps the placeholder's id (`pending:<nonce>` → `turnId`) and replaces the local `startedAt` with the server's authoritative value. Similar pattern for `tool_decision` → server `dispatchToolDecision` → `tool_decision_applied { toolUseId, decision, timestamp }`, which `reduceClaudeMsg` applies via `patchToolBlock` so other connected tabs converge on the server's decision. Linear / Figma multiplayer pattern — optimistic UI without breaking server-as-truth |
| Stale trailing turn finalization (server restart UX) | `Loader.Load` reads `done` from the **snapshot's** trailing turn (NOT the merged Turn — `claudehistory.Parse` force-sets `done=true` on every jsonl turn, which would hide the zombie). Stored as `staleTrailing` boolean. If true, `finalizeStaleTrailingTurn` runs POST-merge: `Done=true, IsError=true, FinishedAt=now`, append text block "Server restarted while this turn was running. The runner was killed; reply was not delivered." Frontend already styles `isError` turns red and `useElapsed` already cuts off the timer when `finishedAt` is set, so no frontend changes needed for the stale-state UX itself — the change is purely server-side hydrate logic. WS reconnect path: `useSessions` `useEffect([connState])` watches non-`'open'` → `'open'` transitions (with the `prev !== 'connecting'` guard so the very first connect doesn't trigger), clears `turnsLoaded` on every per-session ClaudeState, the loader re-fetches and picks up the finalize. Test: `TestLoad_StaleTrailingTurn_FinalizedDespiteJsonlDoneTrue` |
| Disconnected banner | `ClaudeChatView.tsx` renders `.claude-chat__disconnected` (red banner, centered, bold) above the composer when the `disconnected` prop is true. `WorkspacePage.tsx` passes `disconnected={s.connState !== 'open'}` — same expression as `disabled`, but separate props because the comment explicitly marks them as semantically independent (`disabled` gates input; `disconnected` gates the banner — future-proofs degraded-mode where reads still work but writes don't). Styling intentionally differs from `.claude-chat__error` (darker bg, brighter text, centered, bold) — disconnected banner is a stronger signal because the user CAN'T fix the underlying problem from inside the app |

When in doubt: read the regression test for the area before reading the production code. Tests document the bugs the code is shaped against.
